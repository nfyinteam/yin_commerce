<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.nf.shopping.comment.dao.ComImageDao">

    <insert id="addCommentImage" parameterType="edu.nf.shopping.comment.entity.CommentImage">
        insert buy_show_imgs(img_id,com_id,img_index) value(#{imageId},#{comId},#{index})
    </insert>
</mapper>