<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.nf.shopping.comment.dao.ExamineDao">

    <insert id="addExamine" parameterType="edu.nf.shopping.comment.entity.Examine">
        REPLACE into examine(ee_id,ee_type,s_id,com_id,ee_result,ee_time)
        values(#{examineId},#{type},#{staffId},#{comId},#{result},#{time})
    </insert>
</mapper>